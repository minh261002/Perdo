<a href="{{ route('admin.order.edit', $id) }}" class="btn btn-sm btn-primary">
    <i class="ti ti-pencil fs-1"></i>
</a>

<a href="{{ route('admin.order.invoice', $id) }}" class="btn btn-sm btn-outline-primary">
    <i class="ti ti-file fs-1"></i>
</a>
